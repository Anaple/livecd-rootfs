#!/bin/bash

set -ex

# 添加 ROS 2 的软件源
echo "deb http://packages.ros.org/ros2/ubuntu ${LB_DISTRIBUTION} main" > /etc/apt/sources.list.d/ros2.list

# 使用 curl 添加 ROS 2 的 GPG 密钥
curl -sSL https://raw.githubusercontent.com/ros/rosdistro/master/ros.asc | apt-key add -

